<%= turbo_stream.replace "invitation_status" do %>
  <%= render partial: "events/invitation_status_full",
             locals: {
               event: @event,
               participants_by_status: @event.participants_grouped_for(current_user),
               participant_count: @event.participant_count
             } %>
<% end %>
