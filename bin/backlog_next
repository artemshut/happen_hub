#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/backlog_cli"

backlog = BacklogCLI.new
task = backlog.next_task

if task
  puts "#{task.id} #{task.title}"
else
  warn "No tasks available in the Backlog section."
end
